/**:
  ros__parameters:
    use_sim_time: true

# Phase 2 hybrid pipeline parameters
/*/cmd_vel_safety_node:
  ros__parameters:
    risk_level_threshold: 0.8
    high_risk_max_linear_speed: 0.1

/*/health_monitor_node:
  ros__parameters:
    fusion_timeout_sec: 5.0
    heartbeat_timeout_sec: 2.0
